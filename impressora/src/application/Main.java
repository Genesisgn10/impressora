package application;

import entities_classes.ControlTower;
import entities_classes.Duck;
import entities_classes.Plane;
import entities_classes.Superman;

public class Main {

	public static void main(String[] args) {
		
		// Questão 1
		// Flying contract = new Contract("Carlinhos", "Contrato por tempo determinado");
		// Flying photo = new Photo("João", "Retrato");
		// Flying document = new Document("Pedro", "Recibo");
		// Printer printer = new Impressora();
		
		// printer.addPrintable(contract);
		// printer.addPrintable(photo);
		// printer.addPrintable(document);
		// printer.printAll();

		// Questão 2
		// Duck duck = new Duck(100);
		// Plane plane = new Plane();
		// Superman superman = new Superman();
		// ControlTower controlTower = new ControlTower();
		 
		// controlTower.addFlying(duck);
		// controlTower.addFlying(plane);
		// controlTower.addFlying(superman);
		// controlTower.flyAll();
		 
	}
}
